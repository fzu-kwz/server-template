const localUrl = `http://localhost:${process.env.SERVER_PORT}`;
const remoteUrl = `http://124.223.76.46:${process.env.SERVER_PORT}`;
const localCross = "http://localhost:3000";
const remoteCross = "http://124.223.76.46:8000";

export const cross = remoteCross;
export const prefix = remoteUrl;
